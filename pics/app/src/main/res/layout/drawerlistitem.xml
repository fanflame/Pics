<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:material="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:gravity="center" >

    <!--
         <ImageView
        android:id="@id/drag_handle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:background="@drawable/ic_launcher"
        android:visibility="gone" />
    -->

    <com.ran.pics.view.MaterialSytle.LButton
        android:id="@+id/classificationTv"
        android:layout_width="@dimen/drawlayout_width"
        android:layout_height="@dimen/drawlayout_height"
        android:layout_gravity="center"
        android:focusable="false"
        android:focusableInTouchMode="false"
        android:gravity="center"
        android:textSize="16sp"
        android:textColor="@android:color/black"
        material:widget_animColor="#bbff2d6f" />
    <!--
         <TextView
        android:id="@+id/classificationTv"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_gravity="center"
        android:gravity="center"
        android:text="1111"
        android:textColor="@android:color/white" >
    -->
    <!--
         <ImageView
        android:id="@id/click_remove"
        android:layout_width="wrap_content"
        android:layout_height="60dp"
        android:background="@drawable/ic_launcher" />
    -->

</LinearLayout>